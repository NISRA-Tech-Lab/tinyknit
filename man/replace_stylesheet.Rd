% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_stylesheet.R
\name{replace_stylesheet}
\alias{replace_stylesheet}
\title{Replace a local stylesheet link with a remote or inline stylesheet}
\usage{
replace_stylesheet(
  html,
  local_css,
  remote_css = paste0("https://raw.githubusercontent.com/",
    "NISRA-Tech-Lab/rs-resources/", "main/css/style.css"),
  linked_css = basename(local_css),
  timeout = 30,
  warn = TRUE
)
}
\arguments{
\item{html}{A character vector containing HTML. Each element may contain one
or more complete HTML tags.}

\item{local_css}{A length-one character string giving the path to the local
CSS file.}

\item{remote_css}{A length-one character string giving the raw URL from
which the remote CSS should be retrieved.}

\item{linked_css}{The CSS filename or relative path to identify in the HTML.
By default, this is the basename of \code{local_css}.}

\item{timeout}{A positive numeric value giving the connection timeout in
seconds. Defaults to \code{30}.}

\item{warn}{Logical. When \code{TRUE}, warnings are issued if no matching
stylesheet link is found or the remote stylesheet cannot be retrieved.}
}
\value{
A character vector with matching stylesheet links replaced by
either a remote \verb{<link>} element or an inline \verb{<style>} element.
}
\description{
Locates an HTML \verb{<link>} element referencing a local CSS file and compares
the local stylesheet with a remote stylesheet retrieved from a raw URL.
}
\details{
When the normalised local and remote CSS content match, the local link is
replaced with a link to the remote stylesheet. When the files differ, or
when the remote stylesheet cannot be retrieved, the local CSS is embedded
directly in the HTML using a \verb{<style>} element.

The function handles optional whitespace around HTML attribute equals signs
and supports both single-quoted and double-quoted attribute values.

For example, all of the following can be matched:

\if{html}{\out{<div class="sourceCode">}}\preformatted{<link rel="stylesheet" href="style.css">
<link rel = "stylesheet" href = "style.css">
<link href='../css/style.css' rel='stylesheet'>
}\if{html}{\out{</div>}}

CSS comparison normalises:
\itemize{
\item UTF-8 byte-order marks;
\item Windows and Unix line endings;
\item leading and trailing whitespace.
}

Internal CSS whitespace and comments remain significant.

If the remote stylesheet cannot be retrieved, the local stylesheet is
embedded inline. This ensures that the resulting HTML remains usable when
internet access is unavailable.
}
\examples{
html <- c(
  "<!doctype html>",
  "<html>",
  "<head>",
  '<link rel="stylesheet" href="style.css" type="text/css" />',
  "</head>",
  "<body></body>",
  "</html>"
)

\dontrun{
html <- replace_stylesheet(
  html = html,
  local_css = "style.css"
)
}

}
