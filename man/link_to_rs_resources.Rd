% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_to_rs_resources.R
\name{link_to_rs_resources}
\alias{link_to_rs_resources}
\title{Replace local HTML dependencies with rs-resources links or embedded data URIs}
\usage{
link_to_rs_resources(files_dir, output_dir, lines, file_list)
}
\arguments{
\item{files_dir}{A character string giving the relative directory in which
supporting HTML dependency files are stored.}

\item{output_dir}{A character string giving the path to the output directory
containing the rendered HTML dependencies.}

\item{lines}{A character vector of HTML lines to search and modify.}

\item{file_list}{A character vector of file paths available in the
\code{rs-resources} repository.}
}
\value{
A character vector containing the modified HTML lines.
}
\description{
Scans HTML lines for references to local JavaScript, CSS, and image files.
Where a matching JavaScript or CSS file is available in \code{file_list}, the local
reference is replaced with the corresponding hosted file on
\code{rs-resources}. Otherwise, the file is embedded directly into the HTML as a
base64-encoded data URI. PNG images in \code{figure-html} paths are also embedded
as base64 data URIs.
}
\details{
The function looks for lines containing \code{files_dir} and processes three kinds
of assets:
\itemize{
\item JavaScript files (\code{.js})
\item CSS files (\code{.css})
\item PNG images referenced in \code{figure-html}
}

If a JavaScript or CSS file is not found in \code{file_list}, a message is printed
advising the user to contact Tech Lab, and the file is embedded directly in
the HTML.
}
\examples{
\dontrun{
html_lines <- readLines("report.html")
out <- link_to_rs_resources(
  files_dir = "report_files/",
  output_dir = "demo_outputs",
  lines = html_lines,
  file_list = c("/js/example.js", "/css/example.css")
)
}

}
