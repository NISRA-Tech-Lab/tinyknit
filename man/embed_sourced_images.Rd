% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_sourced_images.R
\name{embed_sourced_images}
\alias{embed_sourced_images}
\title{Embed non-inline image sources as base64 data URIs}
\usage{
embed_sourced_images(doc, lines)
}
\arguments{
\item{doc}{A character string. Path to the source \code{.Rmd} file. This is used
to resolve image paths relative to the document directory.}

\item{lines}{A character vector of HTML lines to search and modify.}
}
\value{
A character vector containing the modified HTML lines.
}
\description{
Scans HTML lines for \verb{<img>} tags and replaces image file references with
base64-encoded data URIs, where the image is not already embedded. This helps
create more self-contained HTML output.
}
\details{
The function looks for \verb{<img>} tags in \code{lines}. If an image source does not
already begin with \code{data:image}, the file is read and converted to a base64
data URI using \code{\link[base64enc:base64encode]{base64enc::base64encode()}}.

Multiple \verb{<img>} tags on the same line are processed individually.

Relative paths containing the parent folder name are rewritten using the
directory of \code{doc} before encoding.
}
\examples{
\dontrun{
html_lines <- readLines("report.html")
html_lines <- embed_sourced_images("report.Rmd", html_lines)
}

}
